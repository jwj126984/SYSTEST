<Window x:Class="SIAT.ProjectOperationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SIAT"
        mc:Ignorable="d"
        Title="项目操作"
        Height="380"
        Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Window.Resources>
        <!-- 主要按钮样式 -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="#005A9E"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#004578"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.6"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SuccessButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="BorderBrush" Value="#218838"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1E7E34"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="BorderBrush" Value="#C82333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#BD2130"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 标题 -->
        <StackPanel Grid.Row="0" Margin="20,30,20,20" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="项目管理操作" FontWeight="Bold" FontSize="18" TextAlignment="Center"/>
            <TextBlock Text="请选择要执行的操作" FontSize="14" Foreground="#666666" TextAlignment="Center"/>
        </StackPanel>
        
        <!-- 按钮区域 -->
        <StackPanel Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center" 
                    Orientation="Vertical" Margin="0,10,0,0">
            <Button x:Name="NewProjectButton" Content="新建项目"
                    Style="{StaticResource SuccessButtonStyle}" Margin="0,0,0,20">
                <Button.ToolTip>
                    <TextBlock Text="创建一个新的测试项目"/>
                </Button.ToolTip>
            </Button>
            <Button x:Name="OpenProjectButton" Content="打开项目"
                    Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,0,30">
                <Button.ToolTip>
                    <TextBlock Text="打开已存在的测试项目"/>
                </Button.ToolTip>
            </Button>
        </StackPanel>
        
        <!-- 状态栏 -->
        <StackPanel Grid.Row="2" Margin="0,20,0,15">
            <Border Height="1" Background="#E0E0E0" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="📋" FontSize="14" Margin="0,8,0,0"/>
                <TextBlock Text="项目管理" FontSize="12" Foreground="#666666"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
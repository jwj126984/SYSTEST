<Window x:Class="SIAT.AddEditStepDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SIAT"
        mc:Ignorable="d"
        Title="步骤设置"
        Width="700"
        Height="600"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="0,0,0,5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 步骤名称和类型 -->
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Label Content="步骤名称:" Width="80" />
                <TextBox x:Name="StepNameTextBox" Width="200" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Label Content="步骤类型:" Width="80" />
                <ComboBox x:Name="StepTypeComboBox" Width="150" SelectionChanged="StepTypeComboBox_SelectionChanged">
                    <ComboBoxItem Content="发送并接收" Tag="SendAndReceive" />
                    <ComboBoxItem Content="仅发送" Tag="SendOnly" />
                    <ComboBoxItem Content="仅读取" Tag="ReadOnly" />
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- 发送协议 -->
        <GroupBox x:Name="ProtocolGroupBox" Grid.Row="1" Header="发送协议" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5" VerticalAlignment="Center">
                    <Label Content="协议类型:" Width="80" />
                    <ComboBox x:Name="ProtocolTypeComboBox" Width="150" SelectionChanged="ProtocolTypeComboBox_SelectionChanged">
                        <ComboBoxItem Content="HEX" Tag="HEX" />
                        <ComboBoxItem Content="ASCII" Tag="ASCII" />
                        <ComboBoxItem Content="CAN" Tag="CAN" />
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5" VerticalAlignment="Center" x:Name="CanFdStackPanel" Visibility="Collapsed">
                    <Label Content="CAN FD:" Width="80" />
                    <CheckBox x:Name="IsCanFdCheckBox" />
                </StackPanel>
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5" VerticalAlignment="Center" x:Name="CanIdStackPanel" Visibility="Collapsed">
                    <Label Content="CAN ID:" Width="80" />
                    <TextBox x:Name="CanIdTextBox" Width="150" />
                </StackPanel>
                <StackPanel Grid.Row="3" Margin="5">
                    <Label Content="协议内容:" />
                    <TextBox x:Name="ProtocolContentTextBox" Width="600" Height="100" TextWrapping="Wrap" AcceptsReturn="True" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- 结果变量 -->
        <GroupBox Grid.Row="2" Header="结果变量" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ListView x:Name="ResultVarListView" Grid.Row="0" Margin="5" SelectionChanged="ResultVarListView_SelectionChanged" MouseDoubleClick="ResultVarListView_MouseDoubleClick">
                    <ListView.View>
                    <GridView>
                        <GridViewColumn Width="100" Header="变量名称" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Width="50" Header="单位" DisplayMemberBinding="{Binding Unit}" />
                        <GridViewColumn Width="60" Header="起始位" DisplayMemberBinding="{Binding StartBit}" />
                        <GridViewColumn Width="60" Header="末位" DisplayMemberBinding="{Binding EndBit}" />
                        <GridViewColumn Width="60" Header="长度" DisplayMemberBinding="{Binding Length}" />
                        <GridViewColumn Width="80" Header="分辨率" DisplayMemberBinding="{Binding Resolution}" />
                        <GridViewColumn Width="80" Header="偏移量" DisplayMemberBinding="{Binding Offset}" />
                        <GridViewColumn Width="80" Header="大小端" DisplayMemberBinding="{Binding Endian}" />
                        <GridViewColumn Width="100" Header="CAN ID" DisplayMemberBinding="{Binding CanId}" />
                    </GridView>
                </ListView.View>
                </ListView>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5" HorizontalAlignment="Stretch">
                    <Button x:Name="AddResultVarButton" Content="新增变量" Width="80" Margin="0,0,5,0" Click="AddResultVarButton_Click" />
                    <Button x:Name="EditResultVarButton" Content="修改变量" Width="80" Margin="0,0,5,0" Click="EditResultVarButton_Click" IsEnabled="False" />
                    <Button x:Name="DeleteResultVarButton" Content="删除变量" Width="80" Click="DeleteResultVarButton_Click" IsEnabled="False" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- 结果变量参数 -->
        <GroupBox Grid.Row="3" Header="变量参数" Margin="10" x:Name="ResultVarParamsGroupBox" Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5" VerticalAlignment="Center">
                    <Label Content="起始位:" Width="80" />
                    <TextBox x:Name="StartBitTextBox" Width="100" />
                </StackPanel>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5" VerticalAlignment="Center" x:Name="EndBitStackPanel">
                    <Label Content="末位:" Width="80" />
                    <TextBox x:Name="EndBitTextBox" Width="100" />
                </StackPanel>
                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="5" VerticalAlignment="Center" x:Name="LengthStackPanel" Visibility="Collapsed">
                    <Label Content="长度:" Width="80" />
                    <TextBox x:Name="LengthTextBox" Width="100" />
                </StackPanel>
                <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="5" VerticalAlignment="Center" x:Name="EndianStackPanel" Visibility="Collapsed">
                    <Label Content="大小端:" Width="80" />
                    <ComboBox x:Name="EndianComboBox" Width="150">
                        <ComboBoxItem Content="BigEndian" Tag="BigEndian" />
                        <ComboBoxItem Content="LittleEndian" Tag="LittleEndian" />
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="5" VerticalAlignment="Center">
                    <Label Content="分辨率:" Width="80" />
                    <TextBox x:Name="ResolutionTextBox" Width="100" />
                </StackPanel>
                <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="5" VerticalAlignment="Center">
                    <Label Content="偏移量:" Width="80" />
                    <TextBox x:Name="OffsetTextBox" Width="100" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- 底部按钮 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="10" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="保存" Width="80" Margin="0,0,10,0" Click="SaveButton_Click" />
            <Button x:Name="CancelButton" Content="取消" Width="80" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>